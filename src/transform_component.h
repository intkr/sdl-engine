#pragma once

// temporary, dont touch until graphics are finished
class TransformComponent {
public:

private:
	void resetSpriteModifiers();
	void playAnimations();
	void applyAnimation(Animation* animation);
};